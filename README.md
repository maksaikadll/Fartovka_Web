# Сайт игры Фартовка

## Version 1.2.1

Современный веб-сайт для игры Фартовка с полноценной OAuth авторизацией через GitHub, Discord и Google.

## 🚀 Запуск проекта

### Вариант 1: Без сервера (GitHub Pages)

Просто откройте `index.html` в браузере. OAuth будет работать в режиме имитации.

### Вариант 2: С сервером (полная функциональность)

#### Предварительные требования
- Node.js 16+
- npm или yarn

#### Установка
```bash
# Клонировать репозиторий
git clone https://github.com/maksaikadll/Fartovka_Web.git
cd Fartovka_Web

# Установить зависимости
npm install

# Скопировать файл с переменными окружения
cp env.example .env

# Заполнить .env реальными ключами (см. инструкции ниже)
```

### Настройка OAuth

#### Режим без сервера (GitHub Pages)
OAuth работает в режиме имитации:
- Перенаправляет на реальные OAuth провайдеры
- Обрабатывает callback локально в браузере
- Создает пользователей в localStorage

**Настройка:**
1. Откройте `oauth-simple.js`
2. Замените `your_*_client_id` на реальные Client ID
3. Установите redirect URI: `https://yourusername.github.io/repository-name/oauth/callback.html`

#### Режим с сервером (полная функциональность)
Для настоящей OAuth интеграции настройте приложения:

#### GitHub OAuth
1. Перейдите в [GitHub Developer Settings](https://github.com/settings/developers)
2. Создайте новое OAuth App
3. Укажите:
   - **Homepage URL**: `http://localhost:3000`
   - **Authorization callback URL**: `http://localhost:3000/auth/github/callback`
4. Скопируйте Client ID и Client Secret в `.env`

#### Discord OAuth
1. Перейдите в [Discord Developer Portal](https://discord.com/developers/applications)
2. Создайте новое приложение
3. В разделе OAuth2 → General:
   - **Redirects**: `http://localhost:3000/auth/discord/callback`
4. Скопируйте Client ID и Client Secret в `.env`

#### Google OAuth
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект
3. Включите Google+ API
4. Создайте учетные данные OAuth 2.0
5. Добавьте URI авторизации: `http://localhost:3000/auth/google/callback`
6. Скопируйте Client ID и Client Secret в `.env`

### Запуск
```bash
# Режим разработки (с автоматической перезагрузкой)
npm run dev

# Продакшн режим
npm start
```

Сервер запустится на `http://localhost:3000`

### Тестирование OAuth без настройки
Если вы не хотите настраивать реальные OAuth приложения, сайт будет работать в режиме имитации:
- Кнопки OAuth перенаправят на реальные сервисы
- После авторизации вы вернетесь с кодом
- Система создаст пользователя локально

## Описание

Этот сайт представляет игру Фартовка с современным дизайном и полноценной OAuth авторизацией. Сайт включает:

- **Hero секция** с анимированным фоном и призывом к действию
- **О игре** - информация об игре и статистика
- **Особенности** - ключевые функции игры
- **Типы карт** - описание всех типов карт
- **Правила игры** - интерактивные вкладки с правилами
- **Скачать** - информация об установке и системных требованиях

## Структура файлов

```
Web/
├── index.html      # Главная HTML страница
├── styles.css      # Стили в стиле Riot Games/Valorant
├── script.js       # JavaScript для интерактивности
└── README.md       # Этот файл
```

## Запуск

### Локальный запуск

1. Откройте `index.html` в браузере
2. Или используйте локальный сервер:

```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000

# Node.js (если установлен http-server)
npx http-server
```

3. Откройте в браузере: `http://localhost:8000`

### Использование с веб-сервером

Просто скопируйте файлы в директорию вашего веб-сервера (Apache, Nginx и т.д.)

## Особенности дизайна

- **Анимации**: Плавные переходы и эффекты при наведении
- **Адаптивность**: Полностью адаптивный дизайн для мобильных устройств
- **Интерактивность**: Анимированные счетчики, вкладки, эффекты ripple
- **Типографика**: Современный шрифт Inter от Google Fonts

## Технологии

- HTML5
- CSS3 (с использованием CSS Grid и Flexbox)
- Vanilla JavaScript (без зависимостей)
- Google Fonts (Inter)

## Браузерная поддержка

- Chrome (последние версии)
- Firefox (последние версии)
- Safari (последние версии)
- Edge (последние версии)

## Кастомизация

### Изменение цветов

В файле `styles.css` измените CSS переменные в `:root`:

```css
:root {
    --primary-color: #ff4655;
    --secondary-color: #0f1923;
    --text-primary: #ece8e1;
}
```

### Добавление контента

Отредактируйте `index.html` для добавления новых секций или изменения контента.

## 🔐 Настройка OAuth (опционально)

Для полноценной работы входа через GitHub, Discord и Google необходимо настроить OAuth приложения:

### GitHub OAuth
1. Перейдите в [GitHub Developer Settings](https://github.com/settings/developers)
2. Создайте новое OAuth App
3. Укажите:
   - **Homepage URL**: `https://yourdomain.com`
   - **Authorization callback URL**: `https://yourdomain.com/oauth/github/callback`
4. Замените `your_github_client_id` в `account.js` на ваш Client ID

### Discord OAuth
1. Перейдите в [Discord Developer Portal](https://discord.com/developers/applications)
2. Создайте новое приложение
3. В разделе OAuth2 → General:
   - **Redirects**: `https://yourdomain.com/oauth/discord/callback`
4. Замените `your_discord_client_id` в `account.js` на ваш Client ID

### Google OAuth
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google+ API
4. Создайте учетные данные OAuth 2.0
5. Добавьте URI авторизации: `https://yourdomain.com/oauth/google/callback`
6. Замените `your_google_client_id` в `account.js` на ваш Client ID

### ⚠️ Важно
Текущая реализация использует имитацию OAuth для демонстрации. Для production необходим сервер для обработки callback'ов и безопасного хранения токенов.

## Лицензия

Часть проекта Фартовка.
